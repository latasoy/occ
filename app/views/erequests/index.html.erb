<% content_for :head do %>
  Run Requests
<% end %> 
<% content_for :navigation do %>
  <%= link_to 'Back', :back %> |
  <%= render :partial => 'environments/home_menu'%>
<% end %> 

<%= render :partial => 'erequests_table', :locals =>
  { :erequests => @erequests } %>

