<%#= javascript_include_tag :defaults %>
<%= form_tag '/lists/run', :id => 'lists_run' unless @lists_run_job_tag %>
<%= submit_tag 'Run' , :disable_with => 'Submitting...'%>
<%= collection_select(:environment, :id, @environments, :id, :name,
  options = {:prompt => "-Select an environment"}, :class =>"environment") %>
<%= label_tag :run_options, 'OATS Options:' %> <%= text_field_tag  :run_options, nil, :size => 40 %>
<% @lists_run_job_tag = 'job' %>
<%#*<input type='submit' value='Check All' onClick="$$('#my_form input.check').each(function(box){box.checked=true});return false;" />%>
<%#*<form id="my_form" action="" />%>
